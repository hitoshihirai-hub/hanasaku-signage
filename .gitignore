# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# SQLite
server/data/*.db
server/data/*.db-journal

# OS
Thumbs.db
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# ── 内部文書（公開リポジトリに入れない） ──────────────
# クライアント向け・調達・社内検討の成果物。製品ではないので公開しない。
# ローカルには残す。配布は個別に手渡しする。
説明資料.html
開発委託仕様書.html
大阪府向け提案資料.html
Society5.0適合資料.html
OTOMO確認資料.html
枯山水ゾーン設備仕様メモ.html
7-23テスト手順書.html

# 設計文書。案件の意思決定状況・確認待ち事項・取引先名を含むため公開しない。
# 開発委託時は個別に手渡しする。
SPEC.md

# サイン看板の元PDF・抽出画像（未発表の展示内容）
*.pdf
sign_p*.png
pdf_pages/

# ── 背景マップ画像（外部ストック素材） ──────────────────
# 購入前の透かし入りカンプや、ライセンス確認前の素材を公開URLに載せない。
# ローカルでのレイアウト確認用。購入・許諾が済んだら個別にこの行を外して
# 正規ファイルをコミットする。
display/assets/bg-map.png
display/assets/bg-map.*
# bg-skyline.png はライセンス購入済み（Pixta 129944827・透かし無し）のため公開する。

# 購入素材の元ファイル（配布物ではないのでリポジトリには入れない）
pixta_*.png
pixta_*.jpg

# ── 動画ファイル ──────────────────────────────────
# 7/23実機テストの撮影記録や、展示用の紹介動画など。
# サイズが大きく（GitHubは100MB/ファイルで拒否）、社内の様子や人物が
# 写るため公開リポジトリには入れない。ファイルは手元にそのまま残る。
*.MOV
*.mov
*.mp4
*.MP4
大阪府の公園魅力紹介動画/
7-23テスト準備ツール.html
7-23当日マニュアル.html
